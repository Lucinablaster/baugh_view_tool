require "spec_helper"

describe BaughViewTool do
  it "has a version number" do
    expect(BaughViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
